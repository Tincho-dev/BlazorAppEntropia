@*@model IEnumerable<Models.Fuente>

@{
    ViewBag.Title = "Index";
}

<h2>Fuentes</h2>
<div style="display:block;">
    <p><a class="btn" href='@Url.Action("Create", "Fuente")'>Crear Fuente &raquo;</a></p>
    <!-- Button trigger modal -->
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal" style="width:100px; float:right; margin-bottom:10px;">
        Manual
    </button>

    <!-- Modal -->

    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title" id="exampleModalLabel" style="color:black">Listado de Fuentes</h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" style="color:black">
                    <p>
                        <h5>En esta vista se muestran las Fuentes que se han creado</h5> <br />

                        <b>Numero de simbolos: </b> Se refiere a la cantidad de simbolos distintos que posee la fuente <br />
                        <b>Cadena Generadora: </b>  Se refiere a la cadena de longitud L que se uso para crear la fuente y
                        reconocer los simbolos que puede generar y su frecuencia de aparicion <br />
                        <b>Entropia Maxima: </b> Es la informacion promedio que tendria la fuente si todos sus simbolos fueran
                        igualmente probales. <br />
                        <b>Entropia de la Fuente: </b> Esta es menor a la Entropia maxima ya que hay simbolos
                        que son mas probables que aparezcan y por tanto con una menor informacion asociada
                        lo que disminuye la informacion promedio de la fuente. <br />
                    </p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>
    <table class="table">
        <tr>
            <th>
                Numero de Simbolos posibles
            </th>
            <th>
                Cadena Generadora
            </th>
            <th>
                Entropia Maxima
            </th>
            <th>
                    Cadena Codificada
                </th>
            <th>
                Entropia de la Fuente
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.N)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CadenaFuente)
                </td>
                <td>
                    @item.EntropiaMaxima.ToString("0.##")
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.CadenaCodificada)
                    </td>
                <td>
                    @item.EntropiaDeLaFuente.ToString("0.##")
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IdFuente }, new { @class = "btn" }) |
                    @Html.ActionLink("Detalle", "Details", new { id = item.IdFuente }, new { @class = "btn" })
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdFuente }, new { @class = "btn" })
                </td>
            </tr>
        }

    </table>
</div>*@